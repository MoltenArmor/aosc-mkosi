#!/bin/sh
set -ue

# Exit when build.
if printf '%s' "$*" | grep -qF 'build'; then
     exit 0
fi

cp --archive --update=none /etc/os-release /usr/lib/os-release
bash /stage2_script.sh
rm -f /stage2_script.sh
